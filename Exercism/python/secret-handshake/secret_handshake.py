def commands(number):
    binary = f'{number:b}'
    secrets = list()
    reverse = False

    if len(binary) >= 5 and binary:
        reverse = True

    binary = binary[-4:]

    for index, char in enumerate(binary):
        if char
    1 = "wink"
    10 = "double blink"
    100 = "close your eyes"
    1000 = "jump"
